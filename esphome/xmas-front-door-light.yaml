packages:
  base: !include generic/sonoff_basic_r3_light.yaml

substitutions:
  device_name: Xmas Front Door Light

esphome:
  name: xmas-front-door-light
  name_add_mac_suffix: false

script:
  - id: !extend go_offline
    then:
      - light.turn_off: light_w
