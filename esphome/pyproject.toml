[project]
name = "hotaru-esphome"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
    "esphome==2025.6.3",
    "poethepoet>=0.36.0",
]

[tool.poe.tasks]
patch = "bash -c 'patch -d .venv/lib/python*/site-packages -p 1 < patches/device-model-202504.patch'"
